import { TabName } from "../route";

type RecmdInfo = {
    infoUrl: string,
    tabName: TabName;
    apiName: string,
    img: string,
    state: string,
    title:string,
}

export default RecmdInfo;